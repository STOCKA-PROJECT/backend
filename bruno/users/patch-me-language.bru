meta {
  name: Patch /users/me language only
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/users/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWTtoken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "language": "CA"
  }
}
