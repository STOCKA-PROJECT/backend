meta {
  name: Create Organization
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/organizations
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{JWTtoken}}
}

body:json {
  {
    "name": "Betlem",
    "slug": "belen"
  }
}
