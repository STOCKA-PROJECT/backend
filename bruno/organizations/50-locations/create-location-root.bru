meta {
  name: Create Location (root)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/organizations/{{orgId}}/locations
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{JWTtoken}}
}

body:json {
  {
    "name": "Almacén central",
    "description": "Sede principal"
  }
}
