meta {
  name: Get Location (with breadcrumb)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/organizations/{{orgId}}/locations/{{locationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWTtoken}}
}
