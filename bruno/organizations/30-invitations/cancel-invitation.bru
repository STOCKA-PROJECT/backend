meta {
  name: Cancel Invitation
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/organizations/{{orgId}}/invitations/{{invitationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWTtoken}}
}
