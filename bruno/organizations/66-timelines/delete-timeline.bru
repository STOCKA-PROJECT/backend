meta {
  name: Delete Timeline
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/organizations/{{orgId}}/timelines/{{timelineId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWTtoken}}
}
