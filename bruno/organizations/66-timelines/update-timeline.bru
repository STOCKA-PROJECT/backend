meta {
  name: Update Timeline (rename)
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/organizations/{{orgId}}/timelines/{{timelineId}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{JWTtoken}}
}

body:json {
  {
    "name": "Lanzamiento 2027"
  }
}
