meta {
  name: Delete Piece (soft-delete)
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/organizations/{{orgId}}/pieces/{{pieceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWTtoken}}
}
