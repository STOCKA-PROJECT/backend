meta {
  name: List Pieces (filter status=PENDING)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/organizations/{{orgId}}/pieces?status=PENDING
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWTtoken}}
}
