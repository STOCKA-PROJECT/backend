meta {
  name: List Pieces (search by q)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/organizations/{{orgId}}/pieces?q=martillo
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWTtoken}}
}
